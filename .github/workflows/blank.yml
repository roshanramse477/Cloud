name: CI
on: push
jobs: 
  test:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v4
     - run: echo "Running Tests"
  Deploy: 
      needs: test
      runs-on: ubuntu-latest
      steps:
        - run: echo "Deploying (only iftest passed.)"
