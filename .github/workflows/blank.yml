name: CI
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        env:
          TOKEN: ${{ secrets. ALL_CAPS }}
        run: |  
           echo "Deploying with token length: ${#TOKEN}"
