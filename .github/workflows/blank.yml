name: CI
on: push
jobs:
  deploy:
   
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        env:
         TOKEN: ${{ secrets .ALL_CAPS}}
        run: echo "Deploying o production."
  
